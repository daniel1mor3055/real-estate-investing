[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (may be slower)
    context_md: Tests derived from CONTEXT.md examples
